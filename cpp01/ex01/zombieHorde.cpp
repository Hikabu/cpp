#include "Zombie.hpp"

Zombie* zombieHorde( int N, std::string name )
{
    Zombie zm;
    std::string *foReturn
    for (int i = 0;  i < N; i++)
        foReturn = zm.resurrect(name);
    return (foReturn);
}